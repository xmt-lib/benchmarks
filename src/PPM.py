from random import Random
import re
import os

from src.run import run_cvc5, run_xmt, run_z3

name = os.path.splitext(os.path.basename(__file__))[0]


## create the sets

with open("src/PPM-0007-200-0.asp") as fd:
    file_str = fd.read()
t: list[tuple[int, int]] = list()
for n in re.finditer(r"(?m)^t\(([^,\)]+)\,([^,\)]+)\)\.", file_str):
    t.append((int(n.group(1)), int(n.group(2))))
p = list()
for li in re.finditer(r"(?m)^p\(([^,\)]+),([^,\)]+)\)\.", file_str):
    p.append((int(li.group(1)), int(li.group(2))))
colour = list()
for col in re.finditer(r"(?m)^colour\(([^,\)]+)\)\.", file_str):
    colour.append(col.group(1))
patternlength = int(re.findall(r"(?m)^patternlength\(([^,\)]+)\)\.", file_str)[0])
max_number = max(t, key=lambda x: x[0])[0]

#  Run the scripts

logic = "UFLIA"
result = "sat"

smt = f"""
(set-info :smt-lib-version 2.6)
(set-logic {logic})
(set-info :source |
Generated by: Pierre Carbonnelle
Generated on: 2026-04-28
Generator: https://github.com/xmt-lib/benchmarks
Application: industrial configuration problem
Target solver: Z3
Publications: https://arxiv.org/abs/2602.19102
Time limit: 300
From the DIRT benchmark used to evaluate grounders
|)
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "random")
(set-info :status {result})

(define-fun number ((x Int)) Bool (<= 1 x {max_number}))
(define-fun smallnumber ((x Int)) Bool (<= 1 x {patternlength}))

(declare-fun patternlengthf () Int)
(declare-fun pf (Int) Int)
{"\n".join([f"(assert (= (pf {sq}) {s}))" for (sq, s) in p])}

(declare-fun solution (Int) Int)
(declare-fun solutionindex (Int) Int)
(declare-fun tf (Int) Int)
{"\n".join([f"(assert (= (tf {sq}) {s}))" for (sq, s) in t])}

; co-domain
(assert (forall ((x Int)) (=> (smallnumber x) (number (pf x)))))
(assert (forall ((x Int)) (=> (smallnumber x) (number (solution x)))))
(assert (forall ((x Int)) (=> (smallnumber x) (number (solutionindex x)))))
(assert (forall ((x Int)) (=> (number x) (number (tf x)))))

; ! SN1, SN2 in smallnumber: (~(SN1  <  SN2) | (solutionindex(SN1)  <  solutionindex(SN2)))
(assert (forall ((SN1 Int) (SN2 Int))
            (=> (and (smallnumber SN1) (smallnumber SN2))
                (or (not (< SN1 SN2))
                    (< (solutionindex SN1) (solutionindex SN2)))
            )))

; ! SN1, N2 in smallnumber: (~(pf(SN1)  <  pf(SN2)) | (solution(SN1)  <  solution(SN2)))
(assert (forall ((SN1 Int) (SN2 Int))
            (=> (and (smallnumber SN1) (smallnumber SN2))
                (or (not (< (pf SN1) (pf SN2)))
                    (< (solution SN1) (solution SN2)))
            )))

; ! X in smallnumber, y in number: y = solutionindex(X) => (tf(y)  =  solution(X))
(assert (forall ((X Int) (y Int))
            (=> (and (smallnumber X) (smallnumber y))
                (=> (= y (solutionindex X))
                    (= (tf y) (solution X))
                ))))

(assert (= patternlengthf {patternlength}))

(check-sat)
"""

run_z3(smt, logic, name, result)
run_cvc5(smt, name, result)

xmt = f"""
    (set-option :backend Z3)

    (declare-fun number (Int) Bool)
    (x-interpret-pred number (x-range 1 {max_number} ))

    (declare-fun smallnumber (Int) Bool)
    (x-interpret-pred smallnumber (x-range 1 {patternlength} ))

    (declare-fun patternlengthf () Int)
    (declare-fun pf (Int) Int)
    (declare-fun solution (Int) Int)
    (declare-fun solutionindex (Int) Int)
    (declare-fun tf (Int) Int)

    ; co-domain
    (assert (forall ((x Int)) (=> (smallnumber x) (number (pf x)))))
    (assert (forall ((x Int)) (=> (smallnumber x) (number (solution x)))))
    (assert (forall ((x Int)) (=> (smallnumber x) (number (solutionindex x)))))
    (assert (forall ((x Int)) (=> (number x) (number (tf x)))))

    ; ! SN1, SN2 in smallnumber: (~(SN1  <  SN2) | (solutionindex(SN1)  <  solutionindex(SN2)))
    (assert (forall ((SN1 Int) (SN2 Int))
                (=> (and (smallnumber SN1) (smallnumber SN2))
                    (or (not (< SN1 SN2))
                        (< (solutionindex SN1) (solutionindex SN2)))
                )))

    ; ! SN1, N2 in smallnumber: (~(pf(SN1)  <  pf(SN2)) | (solution(SN1)  <  solution(SN2)))
    (assert (forall ((SN1 Int) (SN2 Int))
                (=> (and (smallnumber SN1) (smallnumber SN2))
                    (or (not (< (pf SN1) (pf SN2)))
                        (< (solution SN1) (solution SN2)))
                )))

    ; ! X in smallnumber, y in number: y = solutionindex(X) => (tf(y)  =  solution(X))
    (assert (forall ((X Int) (y Int))
                (=> (and (smallnumber X) (smallnumber y))
                    (=> (= y (solutionindex X))
                        (= (tf y) (solution X))
                    ))))

    (x-interpret-const patternlengthf {patternlength})
    (x-interpret-fun pf (x-mapping
        {'\n  '.join(f"(({x[0]}) {x[1]})" for x in p)}
        ))

    (x-interpret-fun tf (x-mapping
        {'\n  '.join(f"(({x[0]}) {x[1]})" for x in t)}
        ))

    (check-sat)
"""
run_xmt(xmt, name, result)

