from random import Random
import re
import os

from src.run import run_cvc5, run_xmt, run_z3

name = os.path.splitext(os.path.basename(__file__))[0]


## create the sets

with open("src/QuasiGroup-d_50-p_60.asp") as fd:
    file_str = fd.read()

state: list[tuple[int, int, int]] = list()
for sq in re.finditer(r"(?m)^state\(([^,\)]+),([^,\)]+),([^,\)]+)\)\.", file_str):
    state.append((int(sq.group(1)), int(sq.group(2)), int(sq.group(3))))

domain: list[int] = list()
for sq in re.finditer(r"(?m)^domain\(([^,\)]+)\)\.", file_str):
    domain.append(int(sq.group(1)))

v: list[tuple[int, int]] = list()
for sq in re.finditer(r"(?m)^v\(([^,\)]+),([^,\)]+)\)\.", file_str):
    v.append((int(sq.group(1)), int(sq.group(2))))

#  Run the scripts

logic = "UFLIA"
result = "sat"

smt = f"""
(set-info :smt-lib-version 2.6)
(set-logic {logic})
(set-info :source |
Generated by: Pierre Carbonnelle
Generated on: 2026-04-28
Generator: https://github.com/xmt-lib/benchmarks
Application: industrial configuration problem
Target solver: Z3
Publications: https://arxiv.org/abs/2602.19102
Time limit: 600
From the DIRT benchmark used to evaluate grounders
|)
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "random")
(set-info :status {result})

; cannot use a datatype because of '<'
(define-fun Domain ((x Int)) Bool (<= 1 x {max(*domain)}))

(define-fun v ((x Int) (y Int)) Bool (or
  {"\n  ".join([f"(and (= x {a}) (= y {b}))" for (a,b) in v])}
))
(define-fun state ((x Int) (y Int) (z Int)) Bool (or
  {"\n  ".join([f"(and (= x {a}) (= y {b}) (= z {c}))" for (a,b,c) in state])}
  false  ; if empty
))
(declare-fun assign (Int Int Int) Bool)
(declare-fun assigned (Int) Bool)

(assert (forall ((V Int))
            (=> (Domain V)
                (= (assigned V)
                   (exists ((x Int) (y Int))
                      (and (Domain x) (Domain y)
                           (assign V x y)))))))

(assert (forall ((V Int) (x1 Int) (x2 Int) (y Int))
            (=> (and (Domain V) (Domain x1) (Domain x2) (Domain y))
                (not (and (assign V x1 y) (assign V x2 y) (< x1 x2))))))

(assert (forall ((V Int) (x Int) (y1 Int) (y2 Int))
            (=> (and (Domain V) (Domain x) (Domain y1) (Domain y2))
                (not (and (assign V x y1) (assign V x y2) (< y1 y2))))))

(assert (forall ((s Int) (x Int) (y Int))
            (=> (and (Domain s) (Domain x) (Domain y))
                (not (and (state x y s) (not (assign s x y)))))))

(assert (forall ((V Int))
            (=> (Domain V)
                (not (not (assigned V))))))


(check-sat)
"""

run_z3(smt, logic, name, result)
run_cvc5(smt, name, result)

xmt = f"""
    (set-option :backend Z3)

    (declare-fun Domain (Int) Bool)
    (x-interpret-pred Domain (x-set {" ".join([f'({i})' for i in domain])}))

    (declare-fun v (Int Int) Bool)
    (declare-fun state (Int Int Int) Bool)
    (declare-fun assign (Int Int Int) Bool)
    (declare-fun assigned (Int) Bool)

    ;    assign(V,X,Y):-v(X,Y),domain(V),not nassign(V,X,Y).
    ;    nassign(V,X,Y):-v(X,Y),domain(V),not assign(V,X,Y).
    ; mean that the domain of `assign` is `domain * v`.
    ; No atoms are generated outside of this domain.
    ; We use this in the assertions below

    (assert (forall ((V Int))
                (=> (Domain V)
                    (= (assigned V)
                    (exists ((x Int) (y Int))
                        (and (v x y)
                            (assign V x y)))))))

    ; :-assign(V,X1,Y),assign(V,X2,Y),X1<X2.
    (assert (forall ((V Int) (x1 Int) (x2 Int) (y Int))
                (=> (and (Domain V) (v x1 y) (v x2 y))
                    (not (and (assign V x1 y) (assign V x2 y) (< x1 x2))))))

    ; :-assign(V,X,Y1),assign(V,X,Y2),Y1<Y2.
    (assert (forall ((V Int) (x Int) (y1 Int) (y2 Int))
                (=> (and (Domain V) (v x y1) (v x y2))
                    (not (and (assign V x y1) (assign V x y2) (< y1 y2))))))

    ; :-state(X,Y,S), not assign(S,X,Y).
    (assert (forall ((s Int) (x Int) (y Int))
                (=> (and (Domain s) (v x y))
                    (not (and (state x y s) (not (assign s x y)))))))

    ; :-domain(V),not assigned(V).
    (assert (forall ((V Int))
                (=> (Domain V)
                    (assigned V))))

    (x-interpret-pred v (x-set {" ".join([f'({" ".join([f"{x}" for x in st])})' for st in v])}))
    (x-interpret-pred state (x-set {" ".join([f'({" ".join([f"{x}" for x in st])})' for st in state])}))

    (check-sat)
"""
run_xmt(xmt, name, result)

