from random import Random
import re
import os

name = os.path.splitext(os.path.basename(__file__))[0]
logic = "UFLIA"
result = "sat"

def smt(number=21):
    smt = f"""(set-info :smt-lib-version 2.6)
(set-logic {logic})
(set-info :source |
Generated by: Pierre Carbonnelle
Generated on: 2026-04-28
Generator: https://github.com/xmt-lib/benchmarks
Application: test of grounders
Target solver: Z3
Publications: https://arxiv.org/abs/2602.19102
Time limit: 300
From the DIRT benchmark used to evaluate grounders
|)
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "crafted")
(set-info :status {result})

(define-fun Node ((x Int)) Bool
  (<= 1 x {number})
)

(declare-fun edge (Int Int) Bool)

(assert (forall ((X Int) (W Int) (Y Int) (Z Int) (T Int))
           (not (and (Node X) (Node W) (Node Y) (Node Z) (Node T)
                     (and (< W X Y Z T)
                          (edge W X) (edge X Y) (edge Y Z) (edge Z T)
                          (edge W Y) (edge W Z) (edge W T)
                          (edge X Z) (edge X T) (edge Y T)
                          )))))

(assert (forall ((X Int) (W Int) (Y Int) (Z Int) (T Int))
           (not (and (Node X) (Node W) (Node Y) (Node Z) (Node T)
                     (and (< W X Y Z T)
                          (not (edge W X)) (not (edge X Y)) (not (edge Y Z)) (not (edge Z T))
                          (not (edge W Y)) (not (edge W Z)) (not (edge W T))
                          (not (edge X Z)) (not (edge X T)) (not (edge Y T))
                          )))))

(check-sat)
(get-model)
(exit)
"""
    return smt

def asp(number=21):
    asp_str = f"""node(1..{number}).
arc(X,Y) :- node(X), node(Y), X < Y.
{{ edge(X, Y) }} :- arc(X, Y).

:- W < X, X < Y, Y < Z, Z < T,
   edge(W,X), edge(W,Y), edge(X,Y), edge(W,Z), edge(Y,Z), edge(X,Z), edge(W,T),
   edge(X,T), edge(Y,T), edge(Z,T),
   node(W), node(X), node(Y), node(Z), node(T).
:- W < X, X < Y, Y < Z, Z < T,
   not edge(W,X), not edge(W,Y), not edge(X,Y), not edge(W,Z), not edge(Y,Z), not edge(X,Z), not edge(W,T),
   not edge(X,T), not edge(Y,T), not edge(Z,T),
   node(W), node(X), node(Y), node(Z), node(T).
"""
    return asp_str


def sli(number=21):
    domain_str = ", ".join(str(i) for i in range(1, number+1))

    return f"""vocabulary V {{
    type Node := {{{domain_str}}}
    edge: Node * Node -> Bool
}}
theory T: V {{
    ~(?W, X, Y, Z, T in Node:
        W < X & X < Y & Y < Z & Z < T &
        edge(W, X) & edge(W, Y) & edge(W, Z) & edge(W, T) &
        edge(X, Y) & edge(X, Z) & edge(X, T) &
        edge(Y, Z) & edge(Y, T) & edge(Z, T)).
    ~(?W, X, Y, Z, T in Node:
        W < X & X < Y & Y < Z & Z < T &
        ~edge(W, X) & ~edge(W, Y) & ~edge(W, Z) & ~edge(W, T) &
        ~edge(X, Y) & ~edge(X, Z) & ~edge(X, T) &
        ~edge(Y, Z) & ~edge(Y, T) & ~edge(Z, T)).
}}
structure S: V {{
}}
procedure main() {{
    stdoptions.nbmodels = 1
    printmodels(modelexpand(T, S))
}}
"""

def idp3(number=21):
    domain_str = "; ".join(str(i) for i in range(1, number+1))

    return f"""vocabulary V {{
    type Node isa int
    edge(Node, Node)
}}
theory T: V {{
    ~(?W[Node], X[Node], Y[Node], Z[Node], T[Node]:
        W < X & X < Y & Y < Z & Z < T &
        edge(W, X) & edge(W, Y) & edge(W, Z) & edge(W, T) &
        edge(X, Y) & edge(X, Z) & edge(X, T) &
        edge(Y, Z) & edge(Y, T) & edge(Z, T)).
    ~(?W[Node], X[Node], Y[Node], Z[Node], T[Node]:
        W < X & X < Y & Y < Z & Z < T &
        ~edge(W, X) & ~edge(W, Y) & ~edge(W, Z) & ~edge(W, T) &
        ~edge(X, Y) & ~edge(X, Z) & ~edge(X, T) &
        ~edge(Y, Z) & ~edge(Y, T) & ~edge(Z, T)).
}}
structure S: V {{
    Node = {{{domain_str}}}
}}
procedure main() {{
    stdoptions.nbmodels = 1
    printmodels(modelexpand(T, S))
}}
"""
